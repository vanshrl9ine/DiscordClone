/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "uploadthing.com"
        ]
    }
}

module.exports = nextConfig
module.exports = {
    images: {
      domains: ['utfs.io'],
    },
  }
  
